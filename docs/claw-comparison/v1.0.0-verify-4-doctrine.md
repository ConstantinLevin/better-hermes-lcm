The rule is **not satisfied**. Confirmed residuals include unstored message revisions, partially sourced summaries, removable loss markers, failed ingestion followed by complete-looking searches, and optional subsystems that certify incomplete or stale evidence.

I reviewed all 70 runtime modules, the earlier audit reports, and the landed fixes. Probes used real functions, in-memory SQLite, injected failures, and mocked provider/filesystem boundaries. **I modified no files.**

The checkout changed during the audit. Unless explicitly annotated, citations below use **`d4fda1d`**. I checked subsequent changes through **`c0e1a2a`** and re-probed affected paths. Concurrent uncommitted edits are excluded; local links may therefore show changed lines.

**P1 — High severity: lost source content, broken recovery, or materially false completeness**

**1. The raw store still discards message-envelope content.**  
**Rules:** (a), (b). **Default:** yes, when incoming messages contain additional fields.  
[store.py:475](/home/agent/projects/agentimprovement/betterlcm/store.py:475), [store.py:538](/home/agent/projects/agentimprovement/betterlcm/store.py:538).

**Probe:** Stored a tool message containing `reasoning_content="DECISION"`, `is_error=True`, and provider metadata. Reading it back returned none of those fields and no omission receipt. Separately, `append_batch` with two messages and one token estimate stored only the first because it uses `zip`.

The reader loses reasoning, outcome metadata, arbitrary envelope fields, or an entire trailing message. The short-estimate case requires a malformed direct API call; the engine normally supplies matching lengths.

**Fix:** Preserve a lossless original envelope alongside indexed projections. Validate batch lengths before writing anything.

**2. Editing an already-ingested list position loses the new content.**  
**Rule:** (a). **Default:** yes, if the host edits or replaces an existing message.  
[engine.py:4653](/home/agent/projects/agentimprovement/betterlcm/engine.py:4653), [engine.py:4767](/home/agent/projects/agentimprovement/betterlcm/engine.py:4767).

**Probe:** Ingested `ORIGINAL`, then ingested the same-length list with that position changed to `CORRECTED`. The store contained only `ORIGINAL`; no failure or revision receipt appeared.

Prefix-identity differences affect replay caching but do not reliably rewind the append cursor. Once the active copy disappears, the corrected content has no durable source.

**Fix:** Track message occurrence identity and revision hashes. Append revisions with explicit lineage rather than treating every position before the cursor as already persisted.

**3. Publication accepts partial source mappings and advances past uncovered rows.**  
**Rules:** (a), (d). **Default:** yes when reconciliation or source mapping omits a consumed message.  
[compaction.py:1046](/home/agent/projects/agentimprovement/betterlcm/compaction.py:1046), [compaction.py:1099](/home/agent/projects/agentimprovement/betterlcm/compaction.py:1099).

**Probe, repeated at `124be4c`:** Used real compaction over three consumed messages, injecting a mapping failure for the middle row. It published sources `[1, 3]`, advanced the frontier to `3`, and returned a summary claiming all three decisions. Row `2` remained stored but belonged to no summary.

The guard rejects **zero** mapped sources, not an incomplete mapping. The new contiguous-prefix recovery fix does not repair an already advanced persisted frontier.

**Fix:** Require a complete, occurrence-preserving mapping for every consumed message before publication. Commit verified coverage and the frontier together.

**4. Publication and cleanup transactions can survive cancellation and commit later.**  
**Rules:** (a), (d). **Default:** publication yes; operator cleanup requires enabling `/lcm`.  
[dag.py:287](/home/agent/projects/agentimprovement/betterlcm/dag.py:287), [command.py:1889](/home/agent/projects/agentimprovement/betterlcm/command.py:1889).

**Probe:** Injecting `RuntimeError` during sidecar publication rolled back correctly. Injecting `KeyboardInterrupt` left the node insert pending; a subsequent successful publication committed the earlier node without its metadata.

In cleanup, interrupting `_archive_chunks_for_messages` after deletion left the transaction active. A later commit made the deletion permanent despite the interrupted operation.

**Fix:** Enclose every transaction step, including commit, in rollback protection for `BaseException`. Use transaction ownership or savepoints that cannot be accidentally committed by another operation.

**5. JSON-key sanitization still overwrites content; duplicate JSON keys also collapse.**  
**Rules:** (b), (d). **Default:** yes.  
[extraction.py:372](/home/agent/projects/agentimprovement/betterlcm/extraction.py:372), [extraction.py:406](/home/agent/projects/agentimprovement/betterlcm/extraction.py:406).

**Probes:**

- `{"a<active_memory>x</active_memory>":"FIRST","a":"SECOND"}` became `{"a":"SECOND"}`. Reversing insertion order preserved both, demonstrating the remaining collision hole.
- Tool arguments `{"k":"FIRST","k":"SECOND"}` became `{"k":"SECOND"}`.

The summarizer loses a value without a marker. The ingest-side duplicate-key protection does not protect this later parser.

**Fix:** Reserve the complete original keyspace before renaming keys. Preserve duplicate-key JSON as raw text or as an ordered pair representation.

**6. Structured-content branches still drop outcomes and sibling text.**  
**Rules:** (b), (e). **Default:** yes.  
[extraction.py:201](/home/agent/projects/agentimprovement/betterlcm/extraction.py:201), [extraction.py:238](/home/agent/projects/agentimprovement/betterlcm/extraction.py:238), [extraction.py:247](/home/agent/projects/agentimprovement/betterlcm/extraction.py:247).

**Probes:**

- A typed text object containing success-looking text, `is_error=True`, and `error_code=23` returned only the text.
- A `tool_result` containing `text="stdout"` and `content="stderr failed"` returned `stdout` plus its outcome marker; the second text field disappeared.
- Two qualifying data URIs in one string became a single `[Media attachment]`.

The reader can lose the failure condition, a second output stream, or the number of removed attachments.

**Fix:** Preserve meaningful siblings in every typed branch. Generate a structured removal receipt listing fields and attachment counts, with recovery references.

**7. Some injected-context removal remains entirely unmarked.**  
**Rule:** (b). **Default:** yes.  
[extraction.py:316](/home/agent/projects/agentimprovement/betterlcm/extraction.py:316), [extraction.py:394](/home/agent/projects/agentimprovement/betterlcm/extraction.py:394).

**Probe:** Tool arguments containing `before<active_memory>DECISION</active_memory>after` became `beforeafter`. A self-closing `<active_memory decision="CANCEL"/>` disappeared without a receipt.

The recursive JSON sanitizer disables marking for string values, while additional stripping branches remove tags or attributes without accounting for their content.

**Fix:** Route every removal through one accounting mechanism. Carry receipts outside the sanitized string so nested sanitization cannot suppress them.

**8. Existing loss markers can disappear during later serialization or condensation.**  
**Rule:** (e). **Default:** yes.  
[engine.py:5333](/home/agent/projects/agentimprovement/betterlcm/engine.py:5333), [marked_loss.py:53](/home/agent/projects/agentimprovement/betterlcm/marked_loss.py:53), [engine.py:5902](/home/agent/projects/agentimprovement/betterlcm/engine.py:5902).

**Deterministic probe:** A 20,031-character message contained an injected block. Sanitization replaced 14,031 characters with a receipt, leaving 6,065 characters. Serialization then removed that receipt and reported only **5,785 of 6,065 characters** elided. The final marker understated the original removal.

**Condensation probe:** Child summaries containing the plugin’s excluded-reply receipt were condensed using a valid provider response that omitted it. The parent inherited source links but no exclusion receipt.

**Fix:** Store omission manifests separately from prose. Merge and render them through every summarization and projection stage; never depend on the model to reproduce them.

**9. Active-context cleanup and assembly omit content before counting it, and can drop the receipt itself.**  
**Rules:** (b), (e). **Default:** cleanup yes; ordinary assembly caps are disabled by default, but forced overflow recovery can impose a cap.  
[engine.py:5413](/home/agent/projects/agentimprovement/betterlcm/engine.py:5413), [engine.py:6354](/home/agent/projects/agentimprovement/betterlcm/engine.py:6354), [engine.py:6455](/home/agent/projects/agentimprovement/betterlcm/engine.py:6455).

**Probes:**

- Under capped assembly, a reasoning-only assistant turn disappeared between two user turns without an omission marker.
- Mixed reasoning plus visible assistant content lost the reasoning without a receipt.
- A sufficiently small assembly budget returned only the latest user message; the summary-omission receipt was discarded because it did not fit.

Counting starts after some cleanup has already removed content. Additional tail breaks and leading assistant/tool removal also escape that accounting.

**Fix:** Compute omissions from the original input and reserve an indivisible receipt before allocating the remaining budget.

**10. Tool-pair repair deletes results and invents a recovery claim.**  
**Rules:** (b), (d). **Default:** yes for orphaned, duplicate, late, or split tool sequences.  
[engine.py:5636](/home/agent/projects/agentimprovement/betterlcm/engine.py:5636).

**Probe:** A user message followed by an orphan tool result containing `ACTION FAILED` returned only the user message. Conversely, an assistant call without a result received a stub saying its result was in the earlier context summary, despite an empty DAG.

The reader loses the actual result or is directed to a summary that does not exist.

**Fix:** Preserve a role-valid omission receipt with the original store ID. Missing results must say “unavailable,” with a recovery reference only when one has been verified.

**11. Failed current-turn ingestion is followed by a complete-looking search.**  
**Rule:** (c). **Default:** yes on an ingestion failure.  
[engine.py:3918](/home/agent/projects/agentimprovement/betterlcm/engine.py:3918).

**Probe:** Injected a SQLite write failure while `handle_tool_call("lcm_grep", …)` ingested a message containing `LATEST`. The engine logged the failure, then returned:

```json
{"total_results": 0, "results": [], "complete": true}
```

The reader is told that no matching history exists even though matching content just reached the plugin.

**Fix:** Propagate the ingestion receipt into the tool result. Failed ingestion must make current-history coverage incomplete, even if searching previously stored rows succeeds.

**12. Raw-search bounds are omitted, and recall discards downstream incompleteness.**  
**Rule:** (c). **Default:** yes.  
[store.py:1415](/home/agent/projects/agentimprovement/betterlcm/store.py:1415), [tools.py:2668](/home/agent/projects/agentimprovement/betterlcm/tools.py:2668), [tools.py:4418](/home/agent/projects/agentimprovement/betterlcm/tools.py:4418).

**Probe:** With 100 matching raw rows and `limit=1`, grep returned one result, `total_results=20`, and `complete=true`; the internal candidate cap was invisible.

**Propagation probe:** Gave the real recall FTS arm a search response with `complete=false`, timeout information, and bounded-scan metadata. It returned `([], None)`, losing all failure information because it checks only a top-level `error`.

**Fix:** Return scan progress from raw storage and preserve completeness, failures, and continuation information through every retrieval arm.

**13. Recent history still treats unavailable provenance as an empty, complete window.**  
**Rule:** (c). **Default:** yes on a closed/unavailable DAG or canonical-frontier failure.  
[tools.py:2228](/home/agent/projects/agentimprovement/betterlcm/tools.py:2228), [tools.py:2369](/home/agent/projects/agentimprovement/betterlcm/tools.py:2369).

**Probe, repeated at `c0e1a2a`:** Closed the real DAG, then called `lcm_recent`. It returned zero sections, `complete=true`, and `truncated=false`.

The newly committed fix propagates the work-cap exception, but the closed-connection early return and later canonical-frontier catch still return `[]`.

**Fix:** Use an explicit unavailable/incomplete result for every failed provenance read. Empty must mean a successful scan found nothing.

**14. A truncated index advertises a continuation that does not return the index.**  
**Rules:** (b), (d), (e). **Default:** yes for long index blocks.  
[tools.py:149](/home/agent/projects/agentimprovement/betterlcm/tools.py:149), [tools.py:5479](/home/agent/projects/agentimprovement/betterlcm/tools.py:5479), [dag.py:1024](/home/agent/projects/agentimprovement/betterlcm/dag.py:1024).

**Probe:** Created a 5,999-character index. The response clipped it at 4,000 characters and directed the caller to `lcm_describe`. Following that exact continuation returned neither the index nor the node’s summary text.

The omitted topics have a marker but no working advertised recovery route.

**Fix:** Implement actual index-field pagination in `lcm_describe`, with offsets and completion metadata, before using it as a continuation.

**15. Missing raw sources silently disappear from expansion.**  
**Rules:** (c), (d). **Default:** yes with stale, imported, deleted, or damaged source references.  
[tools.py:1320](/home/agent/projects/agentimprovement/betterlcm/tools.py:1320), [tools.py:1435](/home/agent/projects/agentimprovement/betterlcm/tools.py:1435).

**Probe:** Expanded a leaf whose sole source was missing store ID `999`. The response contained no expanded messages, `total_sources=1`, `remaining_sources=0`, and `has_more=false`, without missing-source or incomplete metadata.

The reader cannot distinguish missing evidence from an exhausted source list. The corresponding missing **child-node** path does report incompleteness.

**Fix:** Report missing raw IDs explicitly and never count an unreadable source as successfully consumed.

**16. Authorization traversal hides retained ancestry behind unreported bounds.**  
**Rules:** (c), (d). **Default:** yes for sufficiently large retained/imported graphs or read failures.  
[tools.py:198](/home/agent/projects/agentimprovement/betterlcm/tools.py:198), [dag.py:516](/home/agent/projects/agentimprovement/betterlcm/dag.py:516).

**Probe:** Gave an old node 257 parents, with its legitimate current-session parent after the first 256. Expansion rejected it as absent from the current session.

The reverse-edge query caps parents at 256; traversal also bounds nodes and hops. Exhaustion and read failure become a negative reachability answer.

**Fix:** Page reverse edges or return “reachability indeterminate.” Preserve the authorized root-to-child path instead of repeatedly rediscovering it under hidden limits.

**17. Bypass receipts still undercount later removals, and compacting a receipt loses its counts.**  
**Rules:** (b), (e). **Default:** bypassed auxiliary/stateless sessions under cap pressure; ordinary foreground sessions are unaffected.  
[ bypass.py:394](/home/agent/projects/agentimprovement/betterlcm/bypass.py:394), [marked_loss.py:24](/home/agent/projects/agentimprovement/betterlcm/marked_loss.py:24). Updated receipt function: [marked_loss.py:45](/home/agent/projects/agentimprovement/betterlcm/marked_loss.py:45) at `124be4c`.

**Probe at `124be4c`:** Ten messages under an extreme cap became an “8 messages dropped” receipt plus `…[LCM cut]`. Nine whole original messages had disappeared; the remaining one was also cut.

Applying `compact_bypass_omission_marker` twice changed the counted receipt into an uncounted generic sentence. The final trim suffix supplies no recovery location by itself.

**Fix:** Maintain cumulative counts from the original messages, make receipt compaction idempotent, and preserve recovery information through the smallest representation.

**18. Recovered spillover bytes are still discarded when the durable copy fails.**  
**Rules:** (a), (d). **Default:** yes on payload-write failure; also reachable through the new recovery-copy opt-out.  
[ingest_protection.py:1390](/home/agent/projects/agentimprovement/betterlcm/ingest_protection.py:1390). Updated fallback: [ingest_protection.py:1439](/home/agent/projects/agentimprovement/betterlcm/ingest_protection.py:1439) at `124be4c`.

**Probe at `124be4c`:** Recovery returned `FULL RECOVERED BODY`; the externalizer returned its storage-failure result, `None`. Ingest returned only the original preview/host-path marker and generation metadata.

The complete bytes reached the plugin but were not retained. When the host spillover file expires, the remaining recovery path is dead.

**Fix:** On failed handoff, store the recovered body inline or fail ingestion without acknowledging durable retention. Keep the copy failure visible.

**19. Filtering and redaction deliberately destroy recoverability under this rule.**  
**Rules:** (a), (b), (d). **Default:** configured message filters and general redaction are off; recognized auxiliary sessions can bypass storage by default. Trajectory protection defaults on when that subsystem is used.  
[engine.py:4670](/home/agent/projects/agentimprovement/betterlcm/engine.py:4670), [placeholder_ledger.py:54](/home/agent/projects/agentimprovement/betterlcm/placeholder_ledger.py:54), [ingest_protection.py:755](/home/agent/projects/agentimprovement/betterlcm/ingest_protection.py:755), [trajectory_store.py:861](/home/agent/projects/agentimprovement/betterlcm/trajectory_store.py:861).

**Probes:** An ignored message became a hash/count placeholder with no stored row. Stateless ingestion returned the original active text but stored nothing. Two different 12-character dummy passwords became identical redaction placeholders. Trajectory protection applies equivalent redaction to the source payload itself.

These are intentional policies, but their hashes and markers do not provide recovery of the original.

**Fix:** Preserve originals in an appropriately protected archive, with resolvable references. Otherwise explicitly treat these configurations as exceptions to the requested invariant.

**20. Rollups can combine one generation’s text with another generation’s provenance, or delete a ready rollup after a failed source read.**  
**Rules:** (a), (c), (d). **Default:** no; temporal rollups are opt-in.  
[rollup_store.py:120](/home/agent/projects/agentimprovement/betterlcm/rollup_store.py:120), [rollup_builder.py:94](/home/agent/projects/agentimprovement/betterlcm/rollup_builder.py:94), [rollup_store.py:577](/home/agent/projects/agentimprovement/betterlcm/rollup_store.py:577).

**Probes:**

- Interleaved a real rollup replacement between reading the rollup row and its sources. The returned ready object contained `FIRST` text/fingerprint with generation two’s source IDs.
- Closed the DAG beneath a previously ready rollup, then ran `build_day`. The unavailable source became “no source,” and the ready rollup was deleted.

**Fix:** Read text and lineage in one consistent snapshot. Treat unavailable source storage as failure, retaining the previous rollup with an explicit stale/failed state.

**21. Adaptive retrieval drops partial-result metadata and can finish with stale “exact” evidence.**  
**Rules:** (b), (c), (d). **Default:** no; adaptive retrieval is opt-in.  
[adaptive_retrieval.py:438](/home/agent/projects/agentimprovement/betterlcm/adaptive_retrieval.py:438), [adaptive_retrieval.py:1053](/home/agent/projects/agentimprovement/betterlcm/adaptive_retrieval.py:1053), [adaptive_retrieval.py:1265](/home/agent/projects/agentimprovement/betterlcm/adaptive_retrieval.py:1265).

**Probe:** A search result with exact evidence, `complete=false`, `has_more=true`, and a structured cursor became an adaptive round with `result_truncated=false` and no continuation lead. After changing the source from tea to coffee, `finish` returned the cached tea quote with status `finished`.

The candidate normalizer also clips long quotes without retaining a truncation receipt.

**Fix:** Preserve upstream incompleteness and cursors. Revalidate source content and metadata at finish; distinguish exact excerpts from completed requirement coverage.

**22. Evidence budgets can remove contradictory candidates while the answer remains “verified” or “sufficient.”**  
**Rules:** (c), (e). **Default:** evidence-pack tools are exposed normally; automatic pre-answer compilation is opt-in.  
[evidence_pack.py:460](/home/agent/projects/agentimprovement/betterlcm/evidence_pack.py:460), [evidence_pack.py:680](/home/agent/projects/agentimprovement/betterlcm/evidence_pack.py:680), [requirements_compiler.py:2034](/home/agent/projects/agentimprovement/betterlcm/requirements_compiler.py:2034).

**Probe:** Supplied distance evidence `Alice=2`, `Bob=3`, and another `Alice=100`. With two allowed references, the difference packet reported `1`, closed coverage, and product verification despite `refs_truncated=true`.

With a one-candidate budget, the requirements compiler answered Alice’s distance as `2` with `answer_sufficient`, without exposing the discarded conflict.

**Fix:** Candidate truncation must prevent closure unless the omitted candidates are proven irrelevant. Completion must describe the original evidence scope, not just admitted operands.

**P2 — Medium severity: optional-index corruption, narrower recovery gaps, and misleading diagnostics**

**23. Assertion freshness excludes source metadata, and default queries ignore expiry.**  
**Rules:** (c), (d). **Default:** no; assertions are opt-in.  
[assertion_store.py:348](/home/agent/projects/agentimprovement/betterlcm/assertion_store.py:348), [assertion_store.py:832](/home/agent/projects/agentimprovement/betterlcm/assertion_store.py:832), [assertion_state.py:61](/home/agent/projects/agentimprovement/betterlcm/assertion_state.py:61).

**Probe:** An assertion with `valid_to=100` remained active/current in a default query. Changing its source role and observation time left its receipt current and returned the old source role.

Readers lose the distinction between current and expired state, and between user and assistant evidence.

**Fix:** Include relevant metadata in dependency receipts and invalidation. Default current-state queries should evaluate validity against an explicit current time.

**24. Query-view dependencies miss observation-time changes.**  
**Rules:** (c), (d). **Default:** no; query views/adaptive retrieval are opt-in.  
[query_view_store.py:301](/home/agent/projects/agentimprovement/betterlcm/query_view_store.py:301), [query_view_store.py:592](/home/agent/projects/agentimprovement/betterlcm/query_view_store.py:592), [query_view_store.py:753](/home/agent/projects/agentimprovement/betterlcm/query_view_store.py:753).

**Probe:** Published a ready query view, changed the source’s `observed_at`, and looked it up again. It remained a cache hit.

The dependency snapshot and triggers cover other source changes but omit this temporal provenance.

**Fix:** Include observation time and its source in dependency fingerprints, generation triggers, publication validation, and lookup validation.

**25. Invalid vector rows are silently skipped under full coverage.**  
**Rule:** (c). **Default:** no; embeddings are opt-in.  
[vector_store.py:1449](/home/agent/projects/agentimprovement/betterlcm/vector_store.py:1449), [vector_store.py:2095](/home/agent/projects/agentimprovement/betterlcm/vector_store.py:2095).

**Probe:** Registered a two-dimensional profile, stored an embedding, then replaced its vector blob with `x'00'`. Full-scan KNN returned no hits with `coverage="full"` and no failure reason.

The reader sees absence even though an eligible source was excluded because its index row could not be decoded.

**Fix:** Count rejected and unreadable vectors separately; report degraded/incomplete coverage whenever eligible rows cannot participate.

**26. Embedding-provider response indices need not be unique or complete.**  
**Rule:** (d). **Default:** no; requires the relevant embedding provider.  
[embedding_provider.py:691](/home/agent/projects/agentimprovement/betterlcm/embedding_provider.py:691), [embedding_provider.py:791](/home/agent/projects/agentimprovement/betterlcm/embedding_provider.py:791).

**Probe:** For two inputs, returned two different vectors both labelled `index=0`. The real provider parser accepted them as the two positional outputs.

A vector can therefore be assigned to a source the response did not identify.

**Fix:** Validate a unique, complete integer permutation of input indices, including contextualized responses; reject missing, duplicate, or invalid indices.

**27. Chunking hides substantial source omissions, including under policy `full`.**  
**Rules:** (b), (c). **Default:** embeddings are off; their default content policy is `conversational`.  
[chunking.py:226](/home/agent/projects/agentimprovement/betterlcm/chunking.py:226).

**Probe:** A 693,889-character multiline tool result under `full` produced one chunk ending at character `2190`, without remaining-coverage metadata. A short message, `Cancel the launch.`, produced no conversational chunk.

Raw storage survives, but semantic retrieval has no indication that these sources were barely indexed or omitted entirely.

**Fix:** Record per-source indexing coverage and exclusions. Make `full` process all content in bounded chunks, and expose omitted-source counts in retrieval coverage.

**28. A missing screenshot makes intact trajectory text unresolvable through its exact reference.**  
**Rule:** (d). **Default:** no; trajectory corpus use is optional.  
[trajectory_store.py:2271](/home/agent/projects/agentimprovement/betterlcm/trajectory_store.py:2271), [trajectory_store.py:3741](/home/agent/projects/agentimprovement/betterlcm/trajectory_store.py:3741).

**Probe:** Inserted an intact textual state with a subsequently unavailable screenshot. Resolving its valid `trajectory://…/state/0` reference raised `TrajectoryAssetError` before returning the text.

The error is explicit, but the advertised provenance route unnecessarily fails for recoverable text.

**Fix:** Resolve textual evidence independently and attach a missing/changed-image marker. Asset failure should not suppress intact fields.

**29. Extraction accepts an empty completion as “nothing to extract,” and longer source lists have incomplete provenance.**  
**Rules:** (c), (d). **Default:** no; pre-compaction extraction is opt-in.  
[extraction.py:454](/home/agent/projects/agentimprovement/betterlcm/extraction.py:454), [extraction.py:470](/home/agent/projects/agentimprovement/betterlcm/extraction.py:470).

**Probe:** An empty provider result returned successful extraction with no note. A 50-source extraction note listed only the first 40 IDs and `(+10 more)`; no durable manifest resolved the other ten.

The reader cannot distinguish a failed/empty generation from an explicit negative finding, or recover the complete note provenance.

**Fix:** Accept only the explicit nonempty no-extraction sentinel as a negative result. Store all source IDs in a resolvable manifest.

**30. Auxiliary evidence selectors accept generation-limit responses without marking them incomplete.**  
**Rule:** (c). **Default:** no for automatic selector paths.  
[host_evidence.py:238](/home/agent/projects/agentimprovement/betterlcm/host_evidence.py:238), [selective_compiler.py:279](/home/agent/projects/agentimprovement/betterlcm/selective_compiler.py:279).

**Probe:** Returned parseable object JSON with `finish_reason="length"` from the provider boundary. Both real selector adapters returned ordinary proposals and usage/provenance without an incomplete indication.

Valid JSON does not establish that selection finished.

**Fix:** Apply the same explicit finish-reason rejection used by summary generation, and carry incomplete generation into fallback status.

**31. Query-interpretation markers miss characters removed by backend normalization.**  
**Rules:** (b), (c). **Default:** yes.  
[search_query.py:68](/home/agent/projects/agentimprovement/betterlcm/search_query.py:68), [search_query.py:279](/home/agent/projects/agentimprovement/betterlcm/search_query.py:279), [tools.py:2962](/home/agent/projects/agentimprovement/betterlcm/tools.py:2962).

**Probe:** Stored `flag∀` and `flag∃`. Searching for `flag∀` returned both, with no query-interpretation marker and `complete=true`.

The backend dropped the distinguishing symbol, while the interpretation reporter examined a different representation.

**Fix:** Derive the interpretation receipt from the exact backend query transformation. Route meaningful unsupported characters through a preserving fallback.

**32. Malformed externalized payloads can load as successful empty content.**  
**Rules:** (c), (d). **Default:** reachable through always-on payload protection as well as opt-in externalization.  
[externalize.py:953](/home/agent/projects/agentimprovement/betterlcm/externalize.py:953).

**Probe:** The real loader accepted JSON containing `{"content_chars":200}` but no content, returning an empty body with the declared size.

A damaged payload can masquerade as a successfully loaded empty result.

**Fix:** Validate schema, content type, declared length, and digest. Return an explicit unavailable/corrupt payload result while preserving its reference.

**33. FTS repair trusts trigger names rather than their behavior.**  
**Rules:** (c), (d). **Default:** yes with a damaged or incorrectly migrated trigger.  
[db_bootstrap.py:2926](/home/agent/projects/agentimprovement/betterlcm/db_bootstrap.py:2926).

**Probe:** Replaced the insert trigger with a same-named no-op. Repair reported no rebuild and no degradation. A subsequent appended `NEEDLE` row existed in storage but was absent from search.

The archive remains present while the index and repair result imply healthy discoverability.

**Fix:** Validate or reinstall canonical trigger definitions transactionally. Verify synchronization with a controlled write/read check.

**34. Doctor can certify coverage after hidden inner caps or without checking child provenance.**  
**Rules:** (c), (d). **Default:** the doctor tool is available normally.  
[coverage_doctor.py:32](/home/agent/projects/agentimprovement/betterlcm/coverage_doctor.py:32), [coverage_doctor.py:86](/home/agent/projects/agentimprovement/betterlcm/coverage_doctor.py:86), [tools.py:6994](/home/agent/projects/agentimprovement/betterlcm/tools.py:6994).

**Probes:**

- A source with 1,000 distinct file entities and a summary containing only the first 400 scored `1.0`, `scan_complete=true`, and pass.
- A parent pointing at missing child node `999` received “all nodes have valid sources” from the orphan check.

The first path caps entities and source text internally. The second checks raw-message edges only and uses a default-capped node listing.

**Fix:** Report truncation at every scan level. Validate both raw and child-node edges, and prohibit a healthy whole-scope result when any scope was skipped.

**35. Backfill count failures become zero remaining work.**  
**Rule:** (c). **Default:** no; operator backfill requires `/lcm` and the relevant feature.  
[command.py:4339](/home/agent/projects/agentimprovement/betterlcm/command.py:4339), [command.py:4555](/home/agent/projects/agentimprovement/betterlcm/command.py:4555).

**Probe:** Injected `sqlite3.OperationalError("locked")` at the read-connection boundary. With equal local pending/processed counters, both remaining-work helpers returned `(0, 0, 0)`.

An unreadable database can therefore look like a completed backfill.

**Fix:** Return unknown/incomplete counts with the failure reason. Local processing counters cannot substitute for a successful corpus scan.

**Fixes that landed during this audit**

These are excluded from the unresolved findings above:

| Change | Audit disposition |
|---|---|
| Sparse DAG recovery used the highest source ID | `b832ea2` replaced it with contiguous-prefix coverage. Finding 3 is the separate publication-time hole. |
| Recovered spillover depended on generic externalization being enabled | `124be4c` removed that default dependency. Finding 18 concerns failed durable handoff. |
| Bypass could delete the newest request to preserve its receipt | The new ordering protects it from that whole-message deletion path. Finding 17 concerns receipt accuracy and further trimming. |
| Tool-call expansion advertised raw-store recovery that returned no tool calls; calls were not charged to the aggregate budget | `c0e1a2a` introduced a supported call offset and shared accounting. I followed the new continuation and observed call bytes advancing. |
| Recent-history work-cap exceptions were swallowed | `c0e1a2a` propagates that exception. Finding 13 is the surviving unavailable-storage path. |
| Lookahead executor depended on incompatible CPython internals; bracketed expand hints broke scaffold detection | The replacement executor and corrected recognizer were reviewed; those original defects are not reissued. |

**Coverage ledger**

“Clean” below applies to the named path, not the whole module. **P** denotes direct execution; **R** denotes code review.

| Modules covered | Paths checked and found clean, or remaining disposition |
|---|---|
| `engine`, `compaction`, `escalation`, `leaf_pipeline`, `prompt_boundary`, `fresh_tail`, `marked_loss`, `errors` | **P:** ordinary publication errors roll back; unmatched calls remain marked in summarizer input; tool-group boundaries remain aligned. **R:** prompt boundary preserves full source text; explicit generation-limit results are rejected; lookahead waits carry deadlines and workers do not publish DAG state. Residuals 2–4, 8–10. |
| `store`, `dag`, `db_bootstrap`, `sqlite_util`, `lifecycle_state`, `reconcile`, `placeholder_ledger`, `node_meta` | **P:** correctly sized batches persist; ordinary new occurrences persist; sidecar index text is stored whole. **R:** `_sqlite_savepoint` handles `BaseException`; observed-time absence remains distinguishable in raw storage. Residuals 1–4, 14–16, 19, 33. |
| `extraction`, `ingest_protection`, `externalize`, `sanitize`, `message_content`, `message_analysis`, `message_patterns`, `tokens` | **P:** outcome-bearing generic tool-result cases retain their outcome; whitespace after data URIs survives; separate attachment-list items remain distinguishable; token counting handles structured arguments. **R:** ordinary media externalization failure retains inline input; regex failure preserves content. Residuals 5–8, 18–19, 29, 32. |
| `aux_session`, `bypass`, `reset_state`, `maintenance`, `host_cooldown`, `model_routing`, `codex_routing`, `session_patterns` | **R:** restricted toolsets alone no longer classify a session as auxiliary; cooldown state resets across sessions; native-compressor abort is recognized; backup uses SQLite backup and staged replacement. Disk backup/restore durability was not executed. Residuals 17, 19; session-generation races already documented remain outside the clean claims. |
| `tools`, `schemas`, `guidance`, `search_query`, `retrieval_core`, `selective_recall`, `occurrence_time` | **P:** ordinary raw-body pagination recovers the body; missing child-node expansion is explicitly incomplete; normal carried ancestry expands. **R:** selective-recall bundles identify themselves as partial; ambiguous occurrence times have explicit unknown states. Residuals 11–16, 31, 34. |
| `rollup_store`, `rollup_builder`, `rollup_periods`, `assertion_store`, `assertion_state`, `assertion_extraction`, `assertion_rebuild`, `query_view_store` | **P:** superseded rollup generations cannot be marked ready; content-invalid assertion receipts and content/role-invalid query views are rejected. **R:** rollup nonconvergence fails explicitly rather than slicing the result. Residuals 20, 23–24; selector finish handling also needs the common fix in 30. |
| `adaptive_retrieval`, `answer_contract`, `evidence_compiler`, `evidence_pack`, `host_evidence`, `preanswer_evidence`, `selective_compiler`, `requirements_compiler`, `reasoning` | **P:** open-cardinality summation remains partial; invalid exact quotes/proposals are rejected or fall back. **R:** explicit feature-disabled and unsupported-contract outcomes are retained. Residuals 21–22, 30. |
| `chunking`, `embedding_provider`, `vector_store`, `trajectory_store` | **P:** normal dimension/nonfinite vector validation rejects invalid inputs; deliberately bounded KNN reports bounded coverage; valid textual trajectory references resolve. **R:** vector write transactions roll back cancellation. Residuals 19, 25–28. |
| `__init__`, `command`, `config`, `presets`, `diagnostics`, `coverage_doctor`, `runtime_identity`, `engine_registry`, `window_scaled_mixin`, `window_scaling` | **R:** optional feature defaults and command registration gates are explicit; registry binding checks session identity; cleanup checks incoming cross-session references; ordinary backup failure aborts cleanup; scaling helpers estimate/select rather than delete source content. **P:** operator failure/count and doctor probes produced findings 4, 34–35. |

Execution covered the failure modes described above without live provider calls or disk mutations. The coverage ledger deliberately does not turn reviewed helper paths, passing unit tests, or an existing marker into a claim that the entire surrounding operation satisfies the rule.