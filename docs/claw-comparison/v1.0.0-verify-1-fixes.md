**12 of the 35 ledger rows are VERIFIED. The other 23 are not closed: 16 PARTIAL, 2 COSMETIC, and 5 BROKEN.** The additionally requested p06 I2 is PARTIAL.

This report covers committed revision **`9161dd6`**. `HEAD` advanced during verification; I inspected that additional commit. Concurrent uncommitted edits are excluded. I modified no files.

I exercised production functions with in-memory SQLite, injected failures, and mocked provider responses. I also ran 33 fork-test functions with storage redirected to memory, plus four occurrence-time tests. This was not a full-suite run; filesystem backup durability and spillover deletion were not tested on disk. Several counterfactual checks loaded pre-fix functions directly from Git into memory.

In the tables, **“Yes” in the test assessment means the test distinguishes some changed behavior—not that it establishes closure.**

The compaction and publication rows resolve as follows.

| Row | Verdict | Implementation evidence and probes | Fork-test assessment |
|---|---|---|---|
| **p05 AX01** | **VERIFIED** | Restricted toolsets alone no longer suffice; the classifier requires additional auxiliary identity. The foreground, prefixed subagent, and explicitly marked child cases behave accordingly. [aux_session.py:506](aux_session.py:506) | **Yes.** The foreground assertion fails before the change. [test_marked_loss.py:770](tests/fork/test_marked_loss.py:770) |
| **p05 CP01** | **VERIFIED** | Published lineage now includes consumed dependent replies, with an explicit exclusion marker. The real compaction fixture retained the dependent row in its leaf; restoring the old compaction function failed that assertion. [compaction.py:1071](compaction.py:1071) | **Yes**, for the mixed-chunk case. The test has conditional assertions, but its current fixture actually consumes a dependent reply; I confirmed the pre-fix failure. [test_marked_loss.py:796](tests/fork/test_marked_loss.py:796) |
| **p05 CP04** | **PARTIAL** | The ordinary no-leaf branch reaches condensation. However, full sweep still stalls when removing the summary scaffold exhausts the raw candidate: that branch breaks without setting `sweep_raw_drained`, which the new condensation branch requires. A scaffold-plus-tail fixture retained all six oversized leaves and reported a no-op. [compaction.py:750](compaction.py:750), [compaction.py:1194](compaction.py:1194) | **Yes**, but it tests ordinary condensation without a replayed scaffold or full sweep. [test_condensation.py:201](tests/fork/test_condensation.py:201) |
| **p05 CP05** | **PARTIAL** | Escalation correctly recomputes remaining time: a fake clock produced route timeouts of 10 and 4 seconds, then stopped dispatching. Ordinary extraction still receives no loop deadline: a configured **one-second** loop passed `timeout_seconds=None`, allowing extraction’s **200-second** default at 1M. Ordinary 256k compaction also deliberately supplies no shared deadline. [escalation.py:399](escalation.py:399), [compaction.py:654](compaction.py:654), [compaction.py:959](compaction.py:959), [engine.py:5098](engine.py:5098) | **Yes**, for condensation forwarding and bounded worker waiting. Neither tests extraction’s remaining deadline gap. [test_condensation.py:141](tests/fork/test_condensation.py:141), [test_parallel_leaf_sweep.py:325](tests/fork/test_parallel_leaf_sweep.py:325) |
| **p05 CP06** | **VERIFIED** | Both optional selection branches now call the aligned selector; forced whole-candidate selection introduces no internal chunk cut. [compaction.py:865](compaction.py:865), [compaction.py:884](compaction.py:884) | **Yes**, for dynamic-path wiring. The test asserts selector invocation, not published call/result membership, and does not exercise full sweep. [test_condensation.py:223](tests/fork/test_condensation.py:223) |
| **p05 CP07** | **VERIFIED** | Cleanup-only blocks lookahead creation and exits before extraction/assertion scheduling. A 1M probe with all three scheduling hooks instrumented recorded **zero** launches. [compaction.py:912](compaction.py:912), [compaction.py:932](compaction.py:932) | **No.** The existing cleanup test also passes with the pre-fix compaction function. Its small-window fixture and disabled extraction never exercise the offending scheduling paths. [test_marked_loss.py:314](tests/fork/test_marked_loss.py:314) |
| **p05 CP08** | **VERIFIED** | Subtraction now uses the corresponding original-pressure span. A real-loop probe with a 10,000-character source replaced by a stub produced exactly `initial − original_span + summary`, rather than subtracting the stub. [compaction.py:1102](compaction.py:1102) | **No corresponding fork regression located** for differing original/replay sizes. Ordinary drain tests do not establish this repair. |
| **p05 CP02/CP03** | **BROKEN** | Node-plus-sidecar rollback works, but publication is still split before invalidation, GC and frontier persistence. Injecting failure immediately after node commit, then retrying in the same process, published two leaves over `[1]`. The new recovery floor also assumes maximum source ID proves contiguous coverage: a leaf over `[1,3]` advanced the restored frontier to 3, hiding unsummarized row 2 from raw-backlog retrieval. Partial mappings remain accepted. [dag.py:287](dag.py:287), [compaction.py:1048](compaction.py:1048), [compaction.py:1096](compaction.py:1096), [engine.py:1966](engine.py:1966) | **Yes**, for sidecar failure and rebinding over a contiguous prefix. Neither test faults the later publication steps, retries without rebinding, or supplies a coverage hole. [test_publication_contract.py:22](tests/fork/test_publication_contract.py:22), [test_publication_contract.py:46](tests/fork/test_publication_contract.py:46) |

The summarizer and extraction rows contain several fixes that are narrower than their original findings.

| Row | Verdict | Implementation evidence and probes | Fork-test assessment |
|---|---|---|---|
| **p05 ES01** | **PARTIAL** | Explicit truncated `choice.finish_reason` is rejected. An actual `status="incomplete"` response normalized to `finish_reason="stop"` is still accepted. This is a real host normalization path, and the plugin does not inspect the surviving incomplete status/details. I reproduced acceptance with that response shape. [escalation.py:274](escalation.py:274), host auxiliary_client.py:6085 (Hermes Agent host internals; path elided) | **Yes**, for explicit `"length"` versus `"stop"`. It does not test Responses-style incomplete status or normalization. [test_summariser_input.py:53](tests/fork/test_summariser_input.py:53) |
| **p05 ES03** | **PARTIAL** | Only the **last** route error becomes the final cause. A primary reporting “maximum context length exceeded” followed by an authentication-failing fallback produced `retry_worthy=False`. Earlier recoverable causes disappear; length/non-index rejection branches also bypass the collected causes. [escalation.py:764](escalation.py:764) | **Yes**, for one repeated capacity failure and manually attached causes. No mixed-route failure test. [test_summariser_input.py:27](tests/fork/test_summariser_input.py:27), [test_summariser_input.py:38](tests/fork/test_summariser_input.py:38) |
| **p05 ES05** | **COSMETIC** | The same focus suffix is discarded. A character count replaces the bare ellipsis, but there is still no source reference or recovery path for an omitted qualifier, including manually supplied focus. [escalation.py:320](escalation.py:320) | **Yes, textually only.** It checks the new marker and character count, not retention or recovery of the qualifier. [test_summariser_input.py:128](tests/fork/test_summariser_input.py:128) |
| **p05 ES06** | **BROKEN** | The blacklist still accepts non-index output: **“Here is your summary.”** succeeded at L1. It also newly rejects a substantive outcome summary: **“I cannot connect to prod: access was revoked; staging deployment succeeded.”** The prefix test confuses summarized failure with refusal to summarize. [escalation.py:630](escalation.py:630), [escalation.py:641](escalation.py:641) | **Yes**, for the enumerated acknowledgements/refusal. No coverage check or legitimate-failure-summary case. [test_summariser_input.py:73](tests/fork/test_summariser_input.py:73) |
| **p05 PB01** | **VERIFIED** | The envelope fitter no longer removes source content. Escape-heavy source round-trips whole even when the supplied allowance is tiny. Capacity failures can still leave work unfinished, but this fitter no longer manufactures a shortened source. [prompt_boundary.py:52](prompt_boundary.py:52) | **Yes.** Exact source equality fails with the old fitter. [test_summariser_input.py:12](tests/fork/test_summariser_input.py:12) |
| **p05 EX01** | **COSMETIC** | The original inter-block decision is still erased; only its replacement text changed. My two-block fixture became a generic “97 chars of injected context removed” marker. The tool-argument path still reduced the same content to `{"body":""}` with no marker. [extraction.py:346](extraction.py:346), [extraction.py:393](extraction.py:393) | **No corresponding fork regression located.** No test establishes preservation of the genuine inter-block decision. |
| **p05 EX02** | **PARTIAL** | Removing whitespace fixes space-separated prose. Padding is still accepted anywhere in the payload class, so a valid padded base64 value immediately followed by `hello world decision` still consumes `hello`. The probe returned only `world decision` plus a media marker. [extraction.py:33](extraction.py:33) | **No corresponding fork regression located** for adjacent prose or padding boundaries. |
| **p05 EX03** | **PARTIAL** | Generic `tool_result` blocks retain selected outcome fields. The dedicated text-block branch still drops siblings and still turns `{"type":"text","text":{"text":"VISIBLE DECISION"}}` into empty text. Attachment count does not preserve attachment identity/order. [extraction.py:206](extraction.py:206), [extraction.py:236](extraction.py:236) | **Yes**, for a generic tool-result block and two images. It misses the dedicated text branch and attachment identity. [test_summariser_input.py:137](tests/fork/test_summariser_input.py:137) |
| **p05 EX04** | **PARTIAL** | Collision protection works in one insertion order only. The report’s original `{"a<active_memory>x</active_memory>":"FIRST","a":"SECOND"}` still becomes `{"a":"SECOND"}`. When the later key is already clean, falling back to its original spelling selects the same occupied key. [extraction.py:380](extraction.py:380) | **No corresponding fork regression located.** The original reproduction still fails. |
| **p05 EX05** | **VERIFIED** | Extraction now separates trusted system instructions from JSON-encoded historical data using the shared envelope. [extraction.py:435](extraction.py:435) | **No dedicated fork test.** The revised ordinary engine test checks system/user roles and the extraction envelope and would fail with the old prompt construction. [test_lcm_engine.py:27476](tests/test_lcm_engine.py:27476) |
| **p05 EX06/EX08** | **PARTIAL** | Provider exceptions and explicit length stops are now failures. But empty output remains successful “nothing to extract”; structured output is stringified and accepted; normalized incomplete responses remain accepted as for ES01. [extraction.py:121](extraction.py:121), [extraction.py:453](extraction.py:453) | **No.** The test injects an exception *after* the old provider-error swallowing boundary. It passes with the pre-fix extraction caller restored; I executed that counterfactual. It never supplies a truncated provider response. [test_summariser_input.py:96](tests/fork/test_summariser_input.py:96) |
| **p05 EX07** | **PARTIAL** | Notes identify only the first 40 source IDs. A 45-source probe omitted IDs 41–45, replacing them with “+5 more,” without a manifest lookup. Mapping failures also deliberately permit an empty source list. The digest alone supplies no recovery endpoint. [extraction.py:471](extraction.py:471), [engine.py:5089](engine.py:5089) | **Yes**, for three explicitly supplied IDs. It tests neither long source lists nor engine-side mapping failure. [test_summariser_input.py:113](tests/fork/test_summariser_input.py:113) |

The remaining rows cover active replay, bypass, retrieval and persistence.

| Row | Verdict | Implementation evidence and probes | Fork-test assessment |
|---|---|---|---|
| **p05 SA01** | **PARTIAL** | Assembly marks entirely removed internal-only tail turns. Partial stripping remains unmarked: `Literal <think>example</think>` becomes `Literal `. Cleanup paths that call the sanitizer directly also bypass the assembly marker. [engine.py:6451](engine.py:6451), [engine.py:5424](engine.py:5424), [compaction.py:1245](compaction.py:1245) | **Yes**, for an entirely removed turn passed directly through assembly. No partial-content or direct-cleanup case. [test_marked_loss.py:200](tests/fork/test_marked_loss.py:200) |
| **p05 BY01** | **BROKEN** | The protected receipt can now displace all actual conversation and still exceed the cap. A 21-message fixture with a 40-token target returned **only a 75-token receipt**, claiming 18 messages dropped although all 21 originals disappeared. Counts are calculated before subsequent trimming and never updated; no exact transcript range is recorded. [bypass.py:375](bypass.py:375), [bypass.py:435](bypass.py:435), [bypass.py:481](bypass.py:481) | **Yes**, for marker survival and zero-budget suffixes. The test checks neither the cap, accurate loss counts, preservation of the newest message, nor recovery. [test_bypass_contract.py:83](tests/fork/test_bypass_contract.py:83) |
| **p05 BY02** | **PARTIAL** | Native unchanged output is no longer always charged, but an unchanged native call that increments its own counter is still counted. Over-cap aborts are still overridden and their abort flag cleared; a warning does not change that outcome. Both paths remain in the changed block. [bypass.py:577](bypass.py:577) | **Yes**, for unchanged output with an unchanged native counter. The test explicitly disables cap pressure, excluding the original destructive-abort reproduction. [test_bypass_contract.py:32](tests/fork/test_bypass_contract.py:32) |
| **p05 BY03** | **PARTIAL** | Inspectable signatures retain supported routing arguments. If signature inspection fails, the helper deliberately returns the old minimal argument set, dropping the chosen route again. Unsupported routing fields can also be dropped without refusing the substitute route. [bypass.py:162](bypass.py:162), [bypass.py:196](bypass.py:196) | **Helper only.** The test never invokes constructor fallback. Reverting its integration while retaining the helper leaves the test green. [test_bypass_contract.py:56](tests/fork/test_bypass_contract.py:56) |
| **p05 SQ01** | **BROKEN** | The description is not derived from the executed query. For `AND rocket`, FTS actually searches `and rocket`, while the new receipt says `AND` was dropped. Conversely, `rocket* 🚀` loses `*` during LIKE sanitization without reporting it. The fix introduces false query provenance. [search_query.py:99](search_query.py:99), [search_query.py:279](search_query.py:279) | **Yes, but it pins the wrong answer.** It asserts that `AND` was dropped without inspecting the actual FTS query. [test_search_honesty.py:84](tests/fork/test_search_honesty.py:84) |
| **p05 SQ02** | **PARTIAL** | Supplementary ideographs now use LIKE and are found. Symbol coverage remains incomplete: `alpha ∑` still selects FTS and becomes `alpha`, silently discarding the mathematical symbol. [search_query.py:30](search_query.py:30), [search_query.py:203](search_query.py:203) | **Yes**, for `𠀀`. It does not test omitted symbol ranges or mixed text/symbol queries. [test_search_honesty.py:18](tests/fork/test_search_honesty.py:18) |
| **p05 SQ03** | **PARTIAL** | DAG searches disclose their cap. Raw-message searches still return at their candidate cap without progress metadata. With 520 matching raw rows, a relevance search inspected 500 and public `lcm_grep` returned `complete:true`, without `bounded_scans`. [store.py:1424](store.py:1424), [tools.py:2698](tools.py:2698), [tools.py:2965](tools.py:2965) | **Yes**, for DAG progress and its propagation. Neither test exercises the raw-message cap. [test_search_honesty.py:38](tests/fork/test_search_honesty.py:38), [test_search_honesty.py:60](tests/fork/test_search_honesty.py:60) |
| **p05 SQ05** | **VERIFIED** | Matching offsets now come from the original string. The original 100 dotted-capital-I prefix fixture retains `NEEDLE` in its snippet. [search_query.py:428](search_query.py:428) | **Yes.** The old offset calculation fails this fixture. [test_search_honesty.py:32](tests/fork/test_search_honesty.py:32) |
| **p05 RS01** | **VERIFIED** | Full session-state reset clears both preflight cleanup flags and the last leaf error. Directly setting and resetting them confirmed this. [reset_state.py:62](reset_state.py:62) | **No corresponding preflight-reset fork test located.** The existing cooldown-reset test concerns the earlier cooldown repair. [test_host_cooldown.py:225](tests/fork/test_host_cooldown.py:225) |
| **p05 AX03** | **PARTIAL** | Database ancestry now walks 256 hops and warns on exhaustion/failure. Stack ancestry still silently stops at 32, and the event cache still silently evicts live records above 4,096. Recording 4,097 starts again made the first child’s lineage disappear. [aux_session.py:635](aux_session.py:635), [aux_session.py:69](aux_session.py:69), [aux_session.py:805](aux_session.py:805) | **No corresponding fork regression located** for the bounded/failed ancestry or cache cases. |
| **p05 MT01/MT02/MT03** | **PARTIAL** | MT01’s exclusive names and MT02’s per-call scratch files fix their reported collisions. MT03 remains: directory-open and directory-`fsync` failures are swallowed indiscriminately. Injected `EIO` from directory `fsync` returned normally, allowing backup success without the promised durability barrier. [maintenance.py:62](maintenance.py:62), [maintenance.py:210](maintenance.py:210), [maintenance.py:98](maintenance.py:98) | **Yes**, for distinct snapshots, unique scratch names and calling `fsync`. The durability test counts calls; it does not inject failure or prove syscall ordering/durability. [test_backup_contract.py:28](tests/fork/test_backup_contract.py:28), [test_backup_contract.py:68](tests/fork/test_backup_contract.py:68), [test_backup_contract.py:88](tests/fork/test_backup_contract.py:88) |
| **p05 OT01/OT02/OT03** | **VERIFIED** | Observation time remains unchanged, the session anchor has its own field, mixed explicit/relative evidence becomes unknown, and relative arithmetic/conversion errors become unknown metadata. [occurrence_time.py:64](occurrence_time.py:64), [occurrence_time.py:110](occurrence_time.py:110), [occurrence_time.py:163](occurrence_time.py:163) | **Yes, outside `tests/fork/`.** The relevant assertions distinguish all three fixes; four relevant test functions passed. [test_occurrence_time.py:10](tests/test_occurrence_time.py:10), [test_occurrence_time.py:66](tests/test_occurrence_time.py:66), [test_occurrence_time.py:78](tests/test_occurrence_time.py:78) |
| **p03 C01** | **VERIFIED** | Inside the deletion transaction, incoming references from retained sessions are checked for both nodes and messages; offending deletion is refused. The retained-parent fixture passed and preserved its sources. [command.py:1914](command.py:1914) | **Yes**, for a retained parent referencing a doomed child node. Direct raw-message references are implemented but not exercised by this test. [test_marked_loss.py:735](tests/fork/test_marked_loss.py:735) |
| **p04 DG1** | **VERIFIED** | Explicit projections repair positional decoding. I constructed the actual legacy order, with `expand_hint` before the time columns: get, FTS search and LIKE search all returned correct bounds and hint. [dag.py:162](dag.py:162), [dag.py:546](dag.py:546), [dag.py:742](dag.py:742) | **No.** The fork test merely appends a trailing column; none of its asserted fields move. Its assertions also pass against the old decoder. [test_hot_paths.py:109](tests/fork/test_hot_paths.py:109) |
| **p02 T04/T16** | **BROKEN** | **T04:** tool-call slices always start at zero, their tokens are not charged to the running budget, and continuation points to raw-store expansion, which still omits tool calls. Three call-only messages returned **120 tokens under a 40-token budget**, with `has_more:false`. **T16:** the new `_RecentIncomplete` is caught by the existing broad handler inside the same helper. A real 4,097-node fixture still returned **`complete:true`, zero sections, `truncated:false`**. [tools.py:1374](tools.py:1374), [tools.py:1421](tools.py:1421), [tools.py:5614](tools.py:5614), [tools.py:2291](tools.py:2291), [tools.py:2329](tools.py:2329) | **T04: yes**, for one short call through node expansion. **T16: only the outer serializer.** Its mock throws from outside the real helper’s swallowing handler, so it cannot detect the surviving defect. [test_marked_loss.py:681](tests/fork/test_marked_loss.py:681), [test_marked_loss.py:707](tests/fork/test_marked_loss.py:707) |
| **p06 I2 — additional requested item** | **PARTIAL** | The recovery helper recognizes spillover paths, but actual ingest does not forward its explicit `hermes_home`. More importantly, successful reads still fail to become durable under the default configuration: `force=True` does not override disabled generic externalization. My successful-read probe retained the expiring host marker and omitted the unique full-output tail. [ingest_protection.py:495](ingest_protection.py:495), [ingest_protection.py:1388](ingest_protection.py:1388), [externalize.py:2114](externalize.py:2114) | **Yes**, for direct helper acceptance. It never ingests the result, deletes the host file, or expands the durable copy. It therefore does not establish the claimed protection against expiry. [test_persisted_output_recovery.py:39](tests/fork/test_persisted_output_recovery.py:39) |

The ledger’s assertion that **every row has a failing-before-change fork test is false**. Besides missing dedicated tests, CP07 and EX06/EX08 survive the relevant counterfactual, and DG1’s fixture does not reproduce the migration defect. [docs/TASKS.md:31](docs/TASKS.md:31)

For **question 1**, these are the unfinished rows ranked by their impact on the no-loss purpose. Rows grouped together have comparable consequences; evidence is in their table entries.

1. **p06 I2:** complete tool output can still expire without a durable copy.
2. **CP02/CP03:** publication recovery can skip an uncovered raw row; failed publication followed by retry still duplicates lineage.
3. **T04/T16:** recovery returns incomplete content with unusable continuation, or certifies an unscannable history as empty.
4. **ES01; EX06/EX08:** unfinished generations can still be accepted as finished summaries/extractions.
5. **EX04; EX01; EX03; EX02:** real source values, decisions, typed outcomes or adjacent prose still disappear before indexing.
6. **BY01; BY02:** bypass still destroys active content without exact recovery provenance; the protected receipt introduces additional failures.
7. **SQ03; SQ02; SQ01:** bounded or altered searches still misrepresent what was searched or established.
8. **MT01/MT02/MT03:** the backup’s directory durability failure can still be acknowledged as success.
9. **EX07:** longer extracted segments lose their complete source manifest.
10. **ES06:** empty indexes remain acceptable, while valid outcome summaries can now be rejected.
11. **SA01:** active-context removal is only partly disclosed.
12. **CP04; CP05; ES03:** condensation, deadlines and capacity rescue remain incomplete, undermining sustained large-window operation.
13. **AX03:** bounded discovery and live-lineage eviction still silently become negative answers.
14. **ES05:** a task-changing focus qualifier remains discarded without a recovery reference.

For **question 2**, the fixes contain these regressions or contradictions:

- **256k condensation now does less than the replaced loop.** `condense_group_cap=1` means one group **total**, not one group at each eligible depth. On the same fixture, the pre-fix function produced a d2 node; current code produced none. The low-anchor test checks only d1 count and misses this. [engine.py:5859](engine.py:5859), [test_condensation.py:127](tests/fork/test_condensation.py:127)

- **The new scheduling predicate is an explicit band switch.** At 262,144 tokens it disables the fork’s deadline and whole-backlog cap; one token later it enables them. My identical 40-message backlog produced a 40-source leaf at 262,144 and a 23-source leaf at 262,145. This directly contradicts smooth behavior between anchors. A new test explicitly requires that Boolean switch. [compaction.py:438](compaction.py:438), [compaction.py:898](compaction.py:898), [test_weighted_chunking.py:206](tests/fork/test_weighted_chunking.py:206)

- **The new index-block cap supplies a dead-end recovery instruction.** It truncates at a fixed 4,000 characters and advertises `lcm_describe(node_id=...)`. That endpoint returns subtree metadata, not the node’s full index block. A 400-topic probe could no longer retrieve topic 399 through the advertised continuation; the test merely checks the instruction’s shape. This affects both anchors. [tools.py:149](tools.py:149), [tools.py:5479](tools.py:5479), [dag.py:1024](dag.py:1024), [test_node_meta.py:212](tests/fork/test_node_meta.py:212)

- **The new tool-call field exceeds expansion budgets and promises unsupported pagination.** The 120-for-40-token reproduction above is a new budget regression, applicable at either window. [tools.py:1374](tools.py:1374), [tools.py:1421](tools.py:1421)

- **The protected bypass receipt can replace the newest user content and remain over budget.** Keeping a receipt is necessary, but this implementation does not reconcile receipt preservation with the cap or truthful loss accounting. [bypass.py:375](bypass.py:375), [bypass.py:435](bypass.py:435)

- **Query interpretation now introduces false provenance, and summary rejection introduces false refusals.** These are the concrete SQ01 and ES06 regressions above, independent of context-window size. [search_query.py:279](search_query.py:279), [escalation.py:654](escalation.py:654)

- **The new frontier floor treats sparse coverage as a complete prefix.** Startup recovery can consequently conceal raw work that the older persisted marker still exposed. [engine.py:1973](engine.py:1973)

The additional `9161dd6` worker-pool replacement passed the real submission, exception, scope-propagation and bounded-wait checks I ran. I found no new regression in that replacement. Its separate token-estimator change does alter 256k fallback behavior—100 CJK characters change from approximately 67 to 151 estimated tokens—so exact low-anchor equivalence cannot be claimed for that path; this comparison does **not** establish that the new estimate is less accurate. [leaf_pipeline.py:98](leaf_pipeline.py:98), [tokens.py:125](tokens.py:125)