You are auditing the OPT-IN subsystems of "better-hermeslcm", a fork of the hermes-lcm plugin (Python, for Hermes Agent) at the repository root.

Purpose of the fork (the yardstick): NO LOSS whatsoever — loss means provenance gone, content unreachable, or a bounded/failed/partial operation reported as complete. Summaries are acceptable only because they are marked, carry provenance and act as an index. A 1,000,000-token window must work well, with every tuning value weighted smoothly between 256k (upstream behaviour kept) and 1M.

Scope — ONLY these subsystems and their callers: rollups (rollup_store, rollup_builder, rollup_periods), assertions (assertion_extraction, assertion_state, assertion_store, assertion_rebuild), adaptive retrieval and query views (adaptive_retrieval, query_view_store, retrieval_core), evidence (evidence_compiler, evidence_pack, selective_compiler, host_evidence, requirements_compiler, answer_contract), trajectory and reasoning (trajectory_store, reasoning), embeddings and chunking (embedding_provider, vector_store, chunk_*, chunking). All are default-off; that does not make their defects acceptable, it makes them lower priority than the always-on paths.

Earlier audits already found many of these: docs/claw-comparison/v1.0.0-p06..p09*.md and v1.0.0-verify-3/-4. Verify each finding against the CURRENT code (several were fixed since), and add anything they missed.

For every finding: cite file:line, give the trigger and whether any default configuration reaches it, state the consequence in terms of what a reader loses or is told falsely, and give a concrete fix direction with an effort rating (S/M/L). Probe the real functions — fault injection, in-memory SQLite, mocked providers. Rank by value to the fork's purpose. Then answer directly: which of these are DEFECTS that must be fixed for the fork to be honest, and which are capability gaps that are fine to leave? Do not modify any files. Write the final report as Markdown.
